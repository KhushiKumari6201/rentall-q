import { ContactPage } from '@/components/contact/ContactPage';

export const metadata = {
  title: 'Contact Us | RentallQ',
  description: 'Reach out to the RentallQ operations team for platform questions, multi-unit onboarding, or custom enterprise solutions.',
};

export default function ContactAppPage() {
  return <ContactPage />;
}
